class NeighborhoodsController < ApplicationController

    #instance method - knows all the avialable listings given a date range

    self.highest_ratio_res_to_listings
    #knows the neighborhood with the higest ratio of reservations to listings
    #doesn't hardcode the neighborhood with the highest ratio

    end 

    self.most_rest 
    #knows the neighborhood with the most reservations 
    #doesn't hardcode the neighborhood with the most reservations
    @reservation.higest

    end 

end
